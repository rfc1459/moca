<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto" android:versionCode="201" android:versionName="2.0.1" package="org.level28.android.moca">

    <!-- System Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- Service permissions -->
    <permission android:name="org.level28.android.moca.permission.SYNC_SERVICE" android:protectionLevel="signature"/>
    <uses-permission android:name="org.level28.android.moca.permission.SYNC_SERVICE"/>

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="16"/>

    <application android:debuggable="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:name=".MocaApp" android:theme="@style/Theme.Moca.Dark">
        <uses-library android:name="com.google.android.maps"/>
        <activity android:launchMode="singleTask" android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:exported="false" android:label="@string/map" android:name=".ui.map.MocaMap"/>
    </application>
</manifest>